const Constants = {
    routes: {
        Home: "/",
        Wifi: "/wifi/",
        TimeSettings: "/time-settings/",
        Display: "/display/",
        NightMode: "/night-mode/"
    }
};

export default Constants;
